// este é o model domain implementado com a interface

export interface Employee {
    // Estabelecer o conjunto de regras pelas quais os dados serão manipulados dentro da aplicação
    id: number
    name: string
    email: string
    phone: string
}
